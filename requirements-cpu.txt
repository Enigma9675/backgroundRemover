# Rembg Background Removal API Dependencies (CPU version)

# Web framework
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.0.0

# Background removal (CPU version - no GPU extras)
rembg>=2.0.50

# Image processing
Pillow>=10.0.0

# HTTP requests (for fetching images from URLs)
requests>=2.31.0

# ONNX Runtime (CPU only)
onnxruntime>=1.16.0
