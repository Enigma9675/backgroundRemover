# web server
flask==3.0.3
flask-cors==4.0.0
gunicorn==21.2.0

# image + ML stack
# Pin numpy <2 until all wheels in your stack are fully compatible
numpy<2.0.0
pillow==10.4.0

# rembg + its runtime
rembg==2.0.55
onnxruntime==1.17.3
